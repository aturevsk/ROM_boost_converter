classdef MLPForExport_mul_2 < nnet.layer.Layer & nnet.layer.Formattable & ...
        nnet.layer.AutogeneratedFromPyTorch
    %MLPForExport_mul_2 Auto-generated custom layer
    % Auto-generated by MATLAB on 28-Mar-2026 19:05:54

    properties (Learnable)
        % Networks (type dlnetwork)

    end

    properties
        % Non-Trainable Parameters

    end

    properties (Learnable)
        % Trainable Parameters

    end

    methods
        function obj = MLPForExport_mul_2(Name, Type, InputNames, OutputNames)
            obj.Name = Name;
            obj.Type = Type;
            obj.NumInputs = 2;
            obj.NumOutputs = 1;
            obj.InputNames = InputNames;
            obj.OutputNames = OutputNames;
        end

        function [mul_8] = predict(obj,mul_6, mul_6_numdims)

            %Use the input format inferred by the importer to permute the input into reverse-PyTorch dimension order
            [mul_6, mul_6_format] = neuralode_mlp_traced.ops.permuteToReversePyTorch(mul_6, '', int64(numel(mul_6_numdims)));
            [mul_6_numdims] = (int64(numel(mul_6_numdims)));

            [mul_8, mul_8_numdims] = tracedPyTorchFunction(obj,mul_6, mul_6_numdims,false,"predict");


            %Permute U-labelled output to forward PyTorch dimension ordering
            if(mul_8_numdims == 1)
                %1D vectors (number of elements in shape = 1) in PyTorch are treated as column vectors in MATLAB
                mul_8 = mul_8(:);
            else

                %Flip dlarray to forward PyTorch ordering
                mul_8 = permute(mul_8, fliplr(1:max(2,mul_8_numdims)));
            end
            mul_8= dlarray(mul_8, repmat('U',1,max(2,mul_8_numdims)));
        end



        function [mul_8] = forward(obj,mul_6, mul_6_numdims)

            %Use the input format inferred by the importer to permute the input into reverse-PyTorch dimension order
            [mul_6, mul_6_format] = neuralode_mlp_traced.ops.permuteToReversePyTorch(mul_6, '', int64(numel(mul_6_numdims)));
            [mul_6_numdims] = (int64(numel(mul_6_numdims)));

            [mul_8, mul_8_numdims] = tracedPyTorchFunction(obj,mul_6, mul_6_numdims,true,"forward");


            %Permute U-labelled output to forward PyTorch dimension ordering
            if(mul_8_numdims == 1)
                %1D vectors (number of elements in shape = 1) in PyTorch are treated as column vectors in MATLAB
                mul_8 = mul_8(:);
            else

                %Flip dlarray to forward PyTorch ordering
                mul_8 = permute(mul_8, fliplr(1:max(2,mul_8_numdims)));
            end
            mul_8= dlarray(mul_8, repmat('U',1,max(2,mul_8_numdims)));
        end



        function [mul_8, mul_8_numdims] = tracedPyTorchFunction(obj,mul_6, mul_6_numdims,isForward,predict)
            [tensorConstantVarTemp2] = dlarray(single([670.78882 5886.1709]));

            [tensorConstantVarTemp2] = tensorConstantVarTemp2(:);
            [mul_8, mul_8_numdims] = neuralode_mlp_traced.ops.pyElementwiseBinary(mul_6,tensorConstantVarTemp2,'times',[mul_6_numdims,1]);
            tensorConstantVarTemp2=[];
        end

    end
end

