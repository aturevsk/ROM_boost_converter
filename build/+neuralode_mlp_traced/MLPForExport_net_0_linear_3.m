classdef MLPForExport_net_0_linear_3 < nnet.layer.Layer & nnet.layer.Formattable & ...
        nnet.layer.AutogeneratedFromPyTorch & nnet.layer.Acceleratable
    %MLPForExport_net_0_linear_3 Auto-generated custom layer
    % Auto-generated by MATLAB on 28-Mar-2026 19:05:53

    properties (Learnable)
        % Networks (type dlnetwork)

    end

    properties
        % Non-Trainable Parameters

    end

    properties (Learnable)
        % Trainable Parameters
        Param_weight
        Param_bias
    end

    methods
        function obj = MLPForExport_net_0_linear_3(Name, Type, InputNames, OutputNames)
            obj.Name = Name;
            obj.Type = Type;
            obj.NumInputs = 1;
            obj.NumOutputs = 2;
            obj.InputNames = InputNames;
            obj.OutputNames = OutputNames;
        end

        function [linear_9, linear_9_numdims] = predict(obj,linear_xu_1)

            %Validates that the input has the correct format and permutes its dimensions into the reverse of the original PyTorch format.
            neuralode_mlp_traced.ops.validateInput(linear_xu_1,'');
            [linear_xu_1, linear_xu_1_format] = neuralode_mlp_traced.ops.permuteInputToReversePyTorch(linear_xu_1, '');

            if ~contains(linear_xu_1_format,'U')
                [linear_xu_1_numdims] = (numel(linear_xu_1_format));
            end

            [linear_9, linear_9_numdims] = tracedPyTorchFunction(obj,linear_xu_1, linear_xu_1_numdims,false,"predict");


            %Permute U-labelled output to forward PyTorch dimension ordering
            if(linear_9_numdims == 1)
                %1D vectors (number of elements in shape = 1) in PyTorch are treated as column vectors in MATLAB
                linear_9 = linear_9(:);
            else

                %Flip dlarray to forward PyTorch ordering
                linear_9 = permute(linear_9, fliplr(1:max(2,linear_9_numdims)));
            end
            linear_9= dlarray(linear_9, repmat('U',1,max(2,linear_9_numdims)));[linear_9_numdims] = ones([1,linear_9_numdims], 'single');
            linear_9_numdims = dlarray(linear_9_numdims,'UU');
        end



        function [linear_9, linear_9_numdims] = forward(obj,linear_xu_1)

            %Validates that the input has the correct format and permutes its dimensions into the reverse of the original PyTorch format.
            neuralode_mlp_traced.ops.validateInput(linear_xu_1,'');
            [linear_xu_1, linear_xu_1_format] = neuralode_mlp_traced.ops.permuteInputToReversePyTorch(linear_xu_1, '');

            if ~contains(linear_xu_1_format,'U')
                [linear_xu_1_numdims] = (numel(linear_xu_1_format));
            end

            [linear_9, linear_9_numdims] = tracedPyTorchFunction(obj,linear_xu_1, linear_xu_1_numdims,true,"forward");


            %Permute U-labelled output to forward PyTorch dimension ordering
            if(linear_9_numdims == 1)
                %1D vectors (number of elements in shape = 1) in PyTorch are treated as column vectors in MATLAB
                linear_9 = linear_9(:);
            else

                %Flip dlarray to forward PyTorch ordering
                linear_9 = permute(linear_9, fliplr(1:max(2,linear_9_numdims)));
            end
            linear_9= dlarray(linear_9, repmat('U',1,max(2,linear_9_numdims)));[linear_9_numdims] = ones([1,linear_9_numdims], 'single');
            linear_9_numdims = dlarray(linear_9_numdims,'UU');
        end



        function [linear_9, linear_9_numdims] = tracedPyTorchFunction(obj,linear_xu_1, linear_xu_1_numdims,isForward,predict)
            [linear_9, linear_9_numdims] = neuralode_mlp_traced.ops.pyLinear(linear_xu_1,obj.Param_weight,obj.Param_bias,[linear_xu_1_numdims,1]);

        end

    end
end

